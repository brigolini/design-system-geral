{
  "name": "@almg/root",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build:interfaces": "cd packages/interfaces && npm run build",
    "build:styles": "cd packages/styles && npm run build",
    "build:native": "cd packages/native && npm run build",
    "build": "npm run build:interfaces && npm run build:styles && npm run build:native",
    "dev": "cd packages/native && npm run dev",
    "test": "cd packages/native && npm run test:run",
    "install:all": "cd packages/interfaces && npm install && cd ../styles && npm install && cd ../native && npm install"
  }
}
